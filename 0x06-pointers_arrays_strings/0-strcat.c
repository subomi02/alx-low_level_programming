#include <stdio.h>
#include <stdlib.h>
#include <string.h>
/**
*_strcat - appends the content of second to first
*@fir:first variable
*@sec:second variable
* Return: the result of the combination
*/
char *_strcat(char *fir, char *sec)
{
	return (strcat(fir, sec));
}
